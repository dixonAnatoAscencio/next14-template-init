import DashboardSkeleton from "../ui/skeletons";

export default function Loading() {//Si no tengo los datos muestra un loading esto se coloca en la ruta , puede ser el esqueleto del dashboard
    return <DashboardSkeleton/>
}